# Unique network role name
- id: "nfs-server_net_role"
  default_mapping: "storage"
  properties:
    subnet: true
    gateway: false
    vip:
       - name: "vip_nfs_server"
         namespace: "haproxy"
         node_roles:
           - "nfs-server"
